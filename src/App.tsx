import * as React from "react";
import "./styles.css";
import {Board} from "./components/Board/Board";

const App: React.FC = () => {
    return (
        <main>
            <Board/>
        </main>
    );
};

export default App;
